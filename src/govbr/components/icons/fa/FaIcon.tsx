import React from 'react'

export const FaIcon = ({ className }: { className: string }) => {
  return (
    // <span className="icon">
    <i className={className} aria-hidden="true"></i>
    // </span>
  )
}
